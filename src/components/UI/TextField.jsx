import React from "react";

export const TextField = ({ dataParentToChild })=>{

        return(
            <div className={"red"}>
                {dataParentToChild}
            </div>
        )
}

